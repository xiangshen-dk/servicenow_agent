# Google Cloud / Vertex AI Configuration
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=your-gcp-project
GOOGLE_CLOUD_LOCATION=us-central1

# ServiceNow Configuration
SERVICENOW_INSTANCE_URL=https://dev123456.service-now.com
SERVICENOW_USERNAME=your_username
# Note: For production deployments, the password will be stored in Google Secret Manager
# During deployment, the password from this file will be automatically uploaded to Secret Manager
# and the deployed agent will fetch it securely from there (not from this file)
SERVICENOW_PASSWORD=your_password

# Agent Configuration
# Format: YYYYMMDD.X (e.g., 20250918.1 for first version on Sept 18, 2025)
AGENT_VERSION=20250918.1

# Note: The deployment script will automatically append the version to the description
AGENT_NAME="ServiceNow Agent"
AGENT_DESCRIPTION="AI agent for managing ServiceNow records through natural language"
AGENT_MODEL=gemini-2.5-flash

# Optional: Customize allowed tables (comma-separated)
# SERVICENOW_ALLOWED_TABLES=incident,change_request,problem,sc_task,sc_req_item,cmdb_ci

# Optional: API settings
# SERVICENOW_API_TIMEOUT=30
# SERVICENOW_MAX_RECORDS=100
